{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "local": "json-server --no-cors --port 8011 --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ronaldo Vargas",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
